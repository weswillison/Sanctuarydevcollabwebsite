[build]
  command = "npm run build"
  publish = "dist"

# Required for React Router: redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[context.branch-deploy]
  command = "npm run build"
